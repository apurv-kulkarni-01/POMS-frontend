import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    brand: {
      1: "#2D3748", //login btn
      2: "#293C54", //search btn
      3: "#E82548", //red
      4: "#38A169", //green
    },
  },
});


export default theme;